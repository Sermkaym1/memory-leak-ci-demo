name: 🔍 Memory Leak Detection CI

on:
  # Основной workflow только по запросу или для полных тестов
  workflow_dispatch:
    inputs:
      test_type:
        description: 'Тип тестов'
        required: true
        default: 'demo'
        type: choice
        options:
          - demo      # Быстрые демо тесты (30 сек)
          - quick     # Быстрые тесты (1 мин)
          - full      # Полные тесты (10 мин)
          - performance # Нагрузочные тесты
  
  # Расписание - каждую ночь в 2:00 UTC
  schedule:
    - cron: '0 2 * * *'

# Разрешения для GitHub Actions
permissions:
  contents: read
  checks: write
  pull-requests: write
  pages: write
  id-token: write

env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1
  ALLURE_RESULTS_DIR: tests/allure-results

jobs:
  # ==========================================
  # JOB 1: Быстрые тесты (всегда)
  # ==========================================
  quick-tests:
    name: ⚡ Быстрые тесты (5 мин)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: � Install Docker Compose
      run: |
        sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose
        docker-compose --version
        
    - name:  Установка зависимостей
      run: |
        chmod +x scripts/install_deps.sh
        ./scripts/install_deps.sh
        
    - name: 🔨 Сборка и запуск сервисов
      run: |
        echo "🔨 Сборка Docker образов..."
        docker-compose build --parallel
        echo "🚀 Запуск сервисов..."
        docker-compose up -d
        echo "⏳ Ожидание готовности сервисов..."
        sleep 60  # Увеличиваем время ожидания
        echo "📊 Проверка статуса контейнеров:"
        docker-compose ps
        echo "🔍 Проверка логов:"
        docker-compose logs --tail=10
        
    - name: 🔍 Проверка готовности сервисов
      run: |
        echo "🌐 Проверяем доступность приложений..."
        for i in {1..30}; do
          if curl -f http://localhost:5000/health 2>/dev/null; then
            echo "✅ App with leak готов"
            break
          fi
          echo "⏳ Ожидание app with leak... ($i/30)"
          sleep 2
        done
        
        for i in {1..30}; do
          if curl -f http://localhost:5001/health 2>/dev/null; then
            echo "✅ App without leak готов"
            break
          fi
          echo "⏳ Ожидание app without leak... ($i/30)"
          sleep 2
        done
        
    - name: ⚡ Запуск быстрых тестов
      run: |
        echo "🧪 Запускаем быстрые тесты на обнаружение утечек..."
        mkdir -p $ALLURE_RESULTS_DIR
        pytest tests/test_quick_demo.py -v -s \
          --alluredir=$ALLURE_RESULTS_DIR \
          --junitxml=quick-test-results.xml || true
          
    - name: 📊 Upload Allure результаты
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-results-quick
        path: ${{ env.ALLURE_RESULTS_DIR }}/
        retention-days: 7
        
    - name: 📋 Публикация результатов тестов
      uses: dorny/test-reporter@v1
      if: always()
      continue-on-error: true
      with:
        name: Quick Tests Results
        path: quick-test-results.xml
        reporter: java-junit
        
    - name: 🧹 Очистка
      if: always()
      run: docker-compose down -v

  # ==========================================
  # JOB 2: Полные тесты (только main)
  # ==========================================
  full-tests:
    name: 🧪 Полные тесты (15 мин)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    if: github.ref == 'refs/heads/main' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.event.inputs.test_type == 'full')
    needs: quick-tests
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: � Install Docker Compose
      run: |
        sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose
        docker-compose --version
        
    - name:  Установка зависимостей
      run: |
        chmod +x scripts/install_deps.sh
        ./scripts/install_deps.sh
        
    - name:  Сборка и запуск сервисов
      run: |
        docker-compose build --parallel
        docker-compose up -d
        sleep 60  # Больше времени для полных тестов
        
    - name: 🧪 Запуск полных тестов
      run: |
        echo "🔍 Запускаем ПОЛНЫЕ тесты на обнаружение утечек памяти..."
        mkdir -p $ALLURE_RESULTS_DIR
        pytest tests/test_memory_leak.py -v -s \
          --alluredir=$ALLURE_RESULTS_DIR \
          --junitxml=full-test-results.xml || true
          
    - name: 📊 Upload Allure результаты
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-results-full
        path: ${{ env.ALLURE_RESULTS_DIR }}/
        retention-days: 30
        
    - name: 📋 Публикация результатов тестов
      uses: dorny/test-reporter@v1
      if: always()
      continue-on-error: true
      with:
        name: Full Tests Results
        path: full-test-results.xml
        reporter: java-junit
        
    - name: 🧹 Очистка
      if: always()
      run: docker-compose down -v

  # ==========================================
  # JOB 3: Генерация Allure отчета
  # ==========================================
  allure-report:
    name: 📊 Allure отчет
    runs-on: ubuntu-latest
    if: always() && (needs.quick-tests.result == 'success' || needs.full-tests.result == 'success')
    needs: [quick-tests, full-tests]
    
    steps:
    - name: 📥 Download Allure результаты
      uses: actions/download-artifact@v4
      with:
        path: allure-results
        merge-multiple: true
      continue-on-error: true
        
    - name: � Проверка наличия результатов
      id: check_results
      run: |
        if [ -d "allure-results" ] && [ "$(find allure-results -name '*.json' | wc -l)" -gt 0 ]; then
          echo "has_results=true" >> $GITHUB_OUTPUT
          echo "✅ Найдены Allure результаты"
        else
          echo "has_results=false" >> $GITHUB_OUTPUT
          echo "⚠️ Allure результаты не найдены, создаем пустой отчет"
          mkdir -p allure-results
          echo '{"name":"No tests","status":"skipped"}' > allure-results/empty.json
        fi
        
    - name: �📊 Генерация Allure отчета
      uses: simple-elf/allure-report-action@master
      if: always()
      with:
        allure_results: allure-results
        allure_report: allure-report
        gh_pages: gh-pages
        allure_history: allure-history
        
    - name: 🚀 Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: allure-history
        destination_dir: allure-report

  # ==========================================
  # JOB 4: Тесты производительности
  # ==========================================
  performance-tests:
    name: ⚡ Тестирование производительности
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.test_type == 'performance'
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
      
    - name: � Install Docker Compose
      run: |
        sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose
        docker-compose --version
      
    - name: �🔨 Сборка и запуск сервисов
      run: |
        docker-compose build --parallel
        docker-compose up -d
        sleep 30
        
    - name: ⚡ Apache Bench нагрузочное тестирование
      run: |
        sudo apt-get update && sudo apt-get install -y apache2-utils
        
        echo "🔥 Нагрузочный тест: Приложение С утечкой"
        ab -n 1000 -c 10 -g leak-app.tsv http://localhost:5000/api/stress > leak-app-results.txt
        
        echo "🔥 Нагрузочный тест: Приложение БЕЗ утечки"  
        ab -n 1000 -c 10 -g no-leak-app.tsv http://localhost:5001/api/stress > no-leak-app-results.txt
        
        echo "📊 Результаты нагрузочного тестирования:"
        echo "=== С УТЕЧКОЙ ==="
        tail -20 leak-app-results.txt
        echo "=== БЕЗ УТЕЧКИ ==="
        tail -20 no-leak-app-results.txt
        
    - name: 📊 Upload результаты производительности
      uses: actions/upload-artifact@v4
      with:
        name: performance-results
        path: |
          *-results.txt
          *.tsv
        retention-days: 7
        
    - name: 🧹 Очистка
      if: always()
      run: docker-compose down -v

  # ==========================================
  # JOB 5: Безопасность
  # ==========================================
  security-scan:
    name: 🔒 Сканирование безопасности
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 🔒 Проверка зависимостей с Safety
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install safety bandit || echo "⚠️ Установка Safety failed, продолжаем..."
        safety check -r requirements.txt --json --output safety-report.json || true
        bandit -r apps/ tests/ -f json -o bandit-report.json || true
        
    - name: 📊 Upload результаты безопасности
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-reports
        path: |
          safety-report.json
          bandit-report.json
        retention-days: 7

  # ==========================================
  # JOB 6: Уведомления
  # ==========================================
  notify:
    name: 📢 Уведомления
    runs-on: ubuntu-latest
    if: always()
    needs: [quick-tests, full-tests, allure-report]
    
    steps:
    - name: 📢 Slack уведомление
      uses: 8398a7/action-slack@v3
      if: always()
      continue-on-error: true
      with:
        status: ${{ job.status }}
        channel: '#memory-leak-ci'
        text: |
          🔍 Memory Leak Detection CI завершен
          
          📊 Статус тестов:
          • Быстрые тесты: ${{ needs.quick-tests.result }}
          • Полные тесты: ${{ needs.full-tests.result }}
          • Allure отчет: ${{ needs.allure-report.result }}
          
          🔗 Отчет: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure-report/
          
          Коммит: ${{ github.sha }}
          Ветка: ${{ github.ref }}
          Автор: ${{ github.actor }}
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        
    - name: 📧 Email уведомление при ошибке
      uses: dawidd6/action-send-mail@v3
      if: failure()
      continue-on-error: true
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.EMAIL_USERNAME }}
        password: ${{ secrets.EMAIL_PASSWORD }}
        subject: "🔴 Memory Leak CI Failed: ${{ github.repository }}"
        to: ${{ secrets.EMAIL_TO }}
        from: Memory Leak CI
        body: |
          Обнаружена ошибка в Memory Leak Detection CI
          
          Репозиторий: ${{ github.repository }}
          Ветка: ${{ github.ref }}
          Коммит: ${{ github.sha }}
          Автор: ${{ github.actor }}
          
          Проверьте логи: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}